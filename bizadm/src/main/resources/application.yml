spring:
  application:
    name: bizadm
  profiles:
    include: pfw, cmn
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration

server:
  port: ${BIZADM_SERVER_PORT:8091}

cpf:
  framework:
    module-id: BIZ
    was-id: ${WAS_ID:bizAP01}
