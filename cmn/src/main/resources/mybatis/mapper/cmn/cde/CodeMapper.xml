<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cpf.cmn.cde.mapper.CodeMapper">

    <!-- CPF 설정 설명입니다. -->
    <select id="findAllCodes" resultType="map">
        SELECT
            code_id,
            parent_id,
            code_key,
            code_value,
            description,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM pfw_code
        ORDER BY parent_id, code_key, code_value
    </select>

    <!-- CPF 설정 설명입니다. -->
    <select id="findCodeByKey" resultType="map" parameterType="string">
        SELECT
            code_id,
            parent_id,
            code_key,
            code_value,
            description,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM pfw_code
        WHERE code_key = #{codeKey}
        ORDER BY code_id
        LIMIT 1
    </select>

    <!-- CPF 설정 설명입니다. -->
    <select id="findCodesByKey" resultType="map" parameterType="string">
        SELECT
            code_id,
            parent_id,
            code_key,
            code_value,
            description,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM pfw_code
        WHERE code_key = #{codeKey}
        ORDER BY code_value, code_id
    </select>

    <!-- CPF 설정 설명입니다. -->
    <select id="findCodeById" resultType="map" parameterType="long">
        SELECT
            code_id,
            parent_id,
            code_key,
            code_value,
            description,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM pfw_code
        WHERE code_id = #{codeId}
    </select>

    <!-- CPF 설정 설명입니다. -->
    <insert id="insertCode" parameterType="cpf.cmn.cde.dto.CommonCodeRequest"
            useGeneratedKeys="true"
            keyProperty="codeId"
            keyColumn="code_id">
        INSERT INTO pfw_code (
            parent_id,
            code_key,
            code_value,
            description,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        ) VALUES (
            #{parentId},
            #{codeKey},
            #{codeValue},
            #{description},
            IFNULL(NULLIF(#{useYn}, ''), 'Y'),
            IFNULL(NULLIF(#{requestUser}, ''), 'SYSTEM'),
            NOW(),
            IFNULL(NULLIF(#{requestUser}, ''), 'SYSTEM'),
            NOW()
        )
    </insert>

    <!-- CPF 설정 설명입니다. -->
    <update id="updateCode">
        UPDATE pfw_code
        SET
            parent_id = #{request.parentId},
            code_key = #{request.codeKey},
            code_value = #{request.codeValue},
            description = #{request.description},
            use_yn = IFNULL(NULLIF(#{request.useYn}, ''), 'Y'),
            updated_by = IFNULL(NULLIF(#{request.requestUser}, ''), 'SYSTEM'),
            updated_at = NOW()
        WHERE code_id = #{codeId}
    </update>

    <!-- CPF 설정 설명입니다. -->
    <delete id="deleteCode" parameterType="long">
        DELETE FROM pfw_code
        WHERE code_id = #{codeId}
    </delete>

</mapper>

