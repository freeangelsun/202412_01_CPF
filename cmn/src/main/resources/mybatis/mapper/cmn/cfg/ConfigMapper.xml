<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cpf.cmn.cfg.mapper.ConfigMapper">

    <!-- ⑤뱺 怨듯넻 ?ㅼ젙媛믪쓣 議고쉶?⑸땲?? -->
    <select id="findAllConfigs" resultType="map">
        SELECT
            config_id,
            config_key,
            config_value,
            config_type,
            description,
            encrypted_yn,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM pfw_config
        ORDER BY config_key
    </select>

    <!-- ?ㅼ젙 ?ㅻ줈 ?ㅼ젙媛믪쓣 議고쉶?⑸땲?? -->
    <select id="findConfigByKey" resultType="map" parameterType="string">
        SELECT
            config_id,
            config_key,
            config_value,
            config_type,
            description,
            encrypted_yn,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM pfw_config
        WHERE config_key = #{configKey}
    </select>

    <!-- ?ㅼ젙 ID濡??ㅼ젙媛믪쓣 議고쉶?⑸땲?? -->
    <select id="findConfigById" resultType="map" parameterType="long">
        SELECT
            config_id,
            config_key,
            config_value,
            config_type,
            description,
            encrypted_yn,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM pfw_config
        WHERE config_id = #{configId}
    </select>

    <!-- 怨듯넻 ?ㅼ젙媛믪쓣 ?깅줉?⑸땲?? ?깅줉 吏곹썑 ?쒕퉬?ㅼ뿉??罹먯떆瑜?由ы봽?덉떆?⑸땲?? -->
    <insert id="insertConfig" parameterType="cpf.cmn.cfg.dto.CommonConfigRequest"
            useGeneratedKeys="true"
            keyProperty="configId"
            keyColumn="config_id">
        INSERT INTO pfw_config (
            config_key,
            config_value,
            config_type,
            description,
            encrypted_yn,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        ) VALUES (
            #{configKey},
            #{configValue},
            IFNULL(NULLIF(#{configType}, ''), 'STRING'),
            #{description},
            IFNULL(NULLIF(#{encryptedYn}, ''), 'N'),
            IFNULL(NULLIF(#{useYn}, ''), 'Y'),
            IFNULL(NULLIF(#{requestUser}, ''), 'SYSTEM'),
            NOW(),
            IFNULL(NULLIF(#{requestUser}, ''), 'SYSTEM'),
            NOW()
        )
    </insert>

    <!-- 怨듯넻 ?ㅼ젙媛믪쓣 ?섏젙?⑸땲?? -->
    <update id="updateConfig">
        UPDATE pfw_config
        SET
            config_key = #{request.configKey},
            config_value = #{request.configValue},
            config_type = IFNULL(NULLIF(#{request.configType}, ''), 'STRING'),
            description = #{request.description},
            encrypted_yn = IFNULL(NULLIF(#{request.encryptedYn}, ''), 'N'),
            use_yn = IFNULL(NULLIF(#{request.useYn}, ''), 'Y'),
            updated_by = IFNULL(NULLIF(#{request.requestUser}, ''), 'SYSTEM'),
            updated_at = NOW()
        WHERE config_id = #{configId}
    </update>

    <!-- 怨듯넻 ?ㅼ젙媛믪쓣 ??젣?⑸땲?? ?ㅼ젣 ?댁쁺?먯꽌???꾩슂 ??use_yn=N 諛⑹떇???쇰━ ??젣濡?諛붽? ???덉뒿?덈떎. -->
    <delete id="deleteConfig" parameterType="long">
        DELETE FROM pfw_config
        WHERE config_id = #{configId}
    </delete>
</mapper>

