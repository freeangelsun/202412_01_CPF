<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cpf.cmn.ref.mapper.CacheRefreshEventMapper">

    <!--
        怨듯넻 罹먯떆 蹂寃??대깽?몃? ?깅줉?⑸땲??
        published_at? 諛由ъ큹 ?⑥쐞 異붿쟻???꾪빐 DATETIME(3)???ъ슜?⑸땲??
    -->
    <insert id="insertEvent">
        INSERT INTO cache_refresh_event (
            cache_name,
            event_type,
            event_key,
            source_was_id,
            published_by,
            published_at,
            created_by,
            updated_by
        ) VALUES (
            #{cacheName},
            #{eventType},
            #{eventKey},
            #{sourceWasId},
            #{publishedBy},
            CURRENT_TIMESTAMP(3),
            #{publishedBy},
            #{publishedBy}
        )
    </insert>

    <!-- ?덈줈 湲곕룞??WAS??湲곗〈 ?대깽?몃? ?ъ쿂由ы븯吏 ?딄린 ?꾪빐 留덉?留??대깽??ID遺???쒖옉?⑸땲?? -->
    <select id="findMaxEventId" resultType="long">
        SELECT MAX(event_id)
        FROM cache_refresh_event
    </select>

    <!-- 媛?WAS媛 留덉?留?泥섎━ ?댄썑 ?대깽?몃쭔 ?쒖꽌?濡?媛?몄샃?덈떎. -->
    <select id="findEventsAfter" resultType="map">
        SELECT
            event_id AS eventId,
            cache_name AS cacheName,
            event_type AS eventType,
            event_key AS eventKey,
            source_was_id AS sourceWasId,
            published_by AS publishedBy,
            published_at AS publishedAt
        FROM cache_refresh_event
        WHERE event_id &gt; #{lastEventId}
        ORDER BY event_id
    </select>
</mapper>

