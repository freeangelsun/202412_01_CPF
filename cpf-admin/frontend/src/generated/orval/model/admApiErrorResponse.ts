/** Controller-source pre-runtime request schema. Authenticated operator fields are server-derived. */
export interface AdmApiErrorResponse {
  code?: string;
  message?: string;
  timestamp?: string;
}
