/** Controller-source pre-runtime request schema. Authenticated operator fields are server-derived. */
export interface AdmApprovalDirectoryEntry {
  jobTitleCode?: string;
  operatorId?: string;
  organizationCode?: string;
  positionCode?: string;
}
