/** Controller-source pre-runtime request schema. Authenticated operator fields are server-derived. */
export interface AdmRoleManagement {
  createdAt?: string;
  description?: string;
  roleId?: string;
  roleName?: string;
  roleType?: string;
  updatedAt?: string;
  useYn?: string;
}
