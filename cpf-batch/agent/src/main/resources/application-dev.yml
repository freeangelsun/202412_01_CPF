server:
  port: ${CPF_PORT:8184}
cpf:
  framework:
    was-id: ${CPF_WAS_ID:cpf-batch-agent-dev-1}
  runtime:
    role: AGENT
  batch:
    control:
      base-url: ${CPF_BATCH_CONTROL_BASE_URL:http://127.0.0.1:8180}
