server:
  port: ${CPF_PORT:8182}
cpf:
  framework:
    was-id: ${CPF_WAS_ID:cpf-batch-worker-test-1}
  runtime:
    role: WORKER
  batch:
    control:
      base-url: ${CPF_BATCH_CONTROL_BASE_URL:http://127.0.0.1:8180}
