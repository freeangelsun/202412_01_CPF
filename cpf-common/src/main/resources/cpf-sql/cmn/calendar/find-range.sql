SELECT calendar_id,business_date,business_day_yn,day_type,institution_code,reason,version_no FROM CMN_BUSINESS_CALENDAR_DAY WHERE calendar_id=? AND business_date BETWEEN ? AND ? ORDER BY business_date
