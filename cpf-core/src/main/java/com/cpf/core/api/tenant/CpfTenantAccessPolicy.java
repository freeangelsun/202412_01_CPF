package com.cpf.core.api.tenant;/** Cross-tenant 접근을 fail-closed로 통제합니다. */public interface CpfTenantAccessPolicy { void verify(String currentTenantId,String resourceTenantId);static CpfTenantAccessPolicy strict(){return (current,resource)->{if(current==null||current.isBlank()||resource==null||resource.isBlank()||!current.equals(resource))throw new SecurityException("Cross-tenant access denied");};}}