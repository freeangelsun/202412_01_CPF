# CPF Documentation Continuity

최종 적용 순서는 `DELETE_COMMAND` -> `APPLY_COMMAND` -> `LOW_COST_VERIFY_COMMAND` -> `GIT_STATUS_COMMAND`이다. 삭제는 ZIP 내부 exact manifest만 사용한다. 새 공식 문서는 `scope.json`의 12 Artifact만 유지하며 추가 DOCX/PDF를 공식 산출물로 만들지 않는다. README Visual 정본 경로는 `cpf-docs/assets/product-docs/`이다.
