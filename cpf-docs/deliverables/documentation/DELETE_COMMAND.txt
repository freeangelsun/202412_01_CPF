# Project Root PowerShell. Reads exact manifest directly from ZIP; does not extract overlay.
$ErrorActionPreference='Stop';$root=(git rev-parse --show-toplevel).Trim();$zip=Join-Path $HOME 'Downloads\CPF_DOCUMENTATION_HARNESS_V1_1_3_FINAL_OVERLAY_20260825_234930.zip';if(!(Test-Path -LiteralPath $zip)){throw "ZIP NOT FOUND: $zip"};Add-Type -AssemblyName System.IO.Compression.FileSystem;$za=[IO.Compression.ZipFile]::OpenRead($zip);try{$e=$za.GetEntry('cpf-docs/deliverables/documentation/DELETE_MANIFEST.txt');if($null-eq$e){throw 'DELETE_MANIFEST missing in ZIP'};$sr=[IO.StreamReader]::new($e.Open());try{$lines=@();while(($line=$sr.ReadLine())-ne$null){$s=$line.Trim();if($s-and-not$s.StartsWith('#')){$lines+=$s}}}finally{$sr.Dispose()};foreach($rel0 in $lines){if([IO.Path]::IsPathRooted($rel0)-or$rel0.Contains('..')-or[Management.Automation.WildcardPattern]::ContainsWildcardCharacters($rel0)){throw "UNSAFE DELETE PATH: $rel0"};$rel=$rel0.Replace('/','\');$p=[IO.Path]::GetFullPath((Join-Path $root $rel));if(!$p.StartsWith($root+[IO.Path]::DirectorySeparatorChar,[StringComparison]::OrdinalIgnoreCase)){throw "OUTSIDE ROOT: $rel0"};if(Test-Path -LiteralPath $p){$item=Get-Item -LiteralPath $p -Force;if($item.PSIsContainer){throw "DELETE_MANIFEST must contain files only: $rel0"};Remove-Item -LiteralPath $p -Force}};$de=$za.GetEntry('cpf-docs/deliverables/documentation/DELETE_EMPTY_DIRS.txt');if($de){$dr=[IO.StreamReader]::new($de.Open());try{$dirs=@();while(($line=$dr.ReadLine())-ne$null){$s=$line.Trim();if($s-and-not$s.StartsWith('#')){$dirs+=$s}}}finally{$dr.Dispose()};foreach($rel0 in $dirs){$p=Join-Path $root $rel0.Replace('/','\');if((Test-Path -LiteralPath $p)-and@(Get-ChildItem -LiteralPath $p -Force).Count-eq0){Remove-Item -LiteralPath $p -Force}}}}finally{$za.Dispose()};Write-Host '[CPF][DOC] exact prior documentation cleanup PASS'
