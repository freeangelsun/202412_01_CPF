# README Dark Preview Review — Harness 2.9.0

- Current README: H1 1 / H2 12 / informative visuals 9.
- Final Hero visual has a directly adjacent visible Korean companion sentence; `validate_readme.py` AI/text companion gate passes.
- Dark brochure preview was re-rendered at **900px, 1200px, 1440px** after the final Hero correction using embedded local PNG data; all 9 visuals load on each surface.
- Major section breathing is intentionally enlarged; numbered H2 transitions are clearly separated from the previous semantic block.
- The previous wide navigation table is removed; reader-task navigation uses bullets, so README table-header wrap is 0 by construction.
- Visual safe-area review: clipped text 0, crop 0, edge collision 0. `capabilities.png` was regenerated so the prior right-edge label pressure is removed.
- Product value is distributed through functional sections: Same JVM/Remote, 4-state failure + UNKNOWN/Reconcile, Batch ownership/recovery, Generator/Starter lifecycle, 12 Public Capability groups, DB3 lifecycle, Gateway/Backoffice ownership, operations trace, phased adoption, and build/test entry.
- Windows VS Code built-in Markdown Preview WebView could not be executed in this Linux container and remains `미검증`; it is not reported as PASS.

**Browser-width visual review: PASS. Windows VS Code native WebView runtime: 미검증.**
