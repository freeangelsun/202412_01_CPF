# CPF 배치 개발 매뉴얼

> **기준 Repository** `freeangelsun/202412_01_CPF`
> **기준 Branch** `master`
> **기준 Commit** `c2e1680fcf42467d445df97f1a3a0c36dab783ef`
> **문서 목적** Spring Batch Job·Step·Tasklet·Chunk, Scheduler, Partition·Remote Worker, Center-Cut, Artifact·Agent와 실행·중단·재시작·대사·복구를 설명한다.
> **주요 독자** Batch 개발자, Scheduler·Worker·Agent 개발자, Batch 운영 API 개발자
> **문서 사용 결과** Batch 기능을 Spring Batch Metadata와 CPF Control 원장에 연결하고 실패·중단·결과 불명·재처리를 판정한다.

## 0. 문서 사용 계약

이 문서는 제품 목표, 기준 Commit의 구현, 실제 실행 검증을 분리한다.

- 목표는 구현·검증 여부와 무관한 제품 계약이다.
- 현재 구현은 Source·SQL·API·Config·Frontend·Script·Test의 exact path로 판정한다.
- 실행하지 않은 Build·DB·Kafka·Browser·다중 인스턴스·장애 시나리오는 `미검증`이다.
- Source에 없는 Class·API·Property·Route·Permission·상태를 만들지 않는다.
- 허용 상태는 `완료`, `부분 구현`, `미구현`, `미검증`, `실패`, `재확인 필요`뿐이다.
- 명령 실행 전 Local Working Tree를 확인하고 기존 변경을 보호한다.


## 1. 현재 구현 판정

| 기능 | 상태 | exact Source | 판정 |
|---|---|---|---|
| Spring Batch Control | `부분 구현` | `CpfSpringBatchExecutionControl` | `JobOperator` start/stop/restart/abandon/recover 연결 확인 |
| CPF 실행 링크 | `부분 구현` | `JdbcBatchExecutionControlPlaneAdapter` | CPF ID와 Spring Job/Step ID 저장 확인 |
| Idempotency | `부분 구현` | `BatchApprovedLaunchRequest`, Adapter `reserve` | Scope·Key·Canonical Request Hash·Plan Checksum 충돌 검증 구현, 3DB/동시성 Runtime 미검증 |
| Fencing | `부분 구현` | `CPF_BATCH_EXECUTION_EPOCH`, Adapter `claimLatestEpoch`·`assertCurrent` | Job별 최신 Epoch와 Bind Transaction Lock 구현, Multi-instance Runtime 미검증 |
| Center-Cut Control | `부분 구현` | `/internal/v1/center-cut` Controller | start/stop/restart/abandon/reconcile API 확인. Reconcile 인증 경계 재확인 필요 |
| Scheduler·Remote Worker | `부분 구현` | execution-runtime·scheduler·worker Source | Source 존재. 실제 Kafka·Multi-instance 실행 미검증 |
| DB V82·V87~V90 | `부분 구현` | Spring Batch Link, Remote Message Ledger, Durable Scheduler Outbox, Idempotency·Deployment Reconciliation 3 Vendor 경로 | 실제 DB 실행 미검증 |
| Build·Runtime | `실패`/`미검증` | Convention Plugin ID·Marker·Included Build 공급 계약 불일치 | empty-cache Plugin Resolution 전 실행 결과 기록 금지 |

## 2. Module 지도

| Module | 책임 |
|---|---|
| `cpf-batch:contract` | Public API·SPI, 실행 계획·Topology·Step Handler 계약 |
| `runtime-common` | Runtime 공통 상태·Client·보안 문맥 |
| `execution-runtime` | Spring Batch Job Materialize, JobOperator Control, Remote Step/Chunk, 실행 링크 |
| `control-server` | 배포·Runtime Lifecycle·Probe |
| `scheduler` | Trigger·Misfire·Dispatch |
| `worker` | Step Handler·Item 처리·원격 Worker |
| `center-cut-runner` | Center-Cut Control API |
| `host-agent` | Artifact 설치·검증·Service·Log Archive |
| `testkit` | Contract·Runtime Test |

## 3. Job·Step 설계

각 Job 정의:

- `jobId`, `jobName`, `definitionVersion`, checksum, Owner Domain
- JobParameter 식별성·기본값·민감정보 금지
- Step 순서와 Restart 가능성
- Tasklet 또는 Chunk 선택 근거
- Reader·Processor·Writer의 Transaction·Idempotency
- Skip·Retry 분류와 최대치
- Commit Interval과 처리량·Lock 영향
- Exit Status와 업무 정상 건수·오류 건수
- Timeout·Stop·Cancellation·Cleanup

## 4. Tasklet·Chunk

### Tasklet

외부 Process·단일 업무 조치에 사용한다. Side Effect 전후 Checkpoint와 Unknown 판정 방법을 기록한다.

### Chunk

```text
Read N → Process N → Write N → Commit
```

- Reader 재시작 위치와 정렬 Key 고정
- Processor 순수성 또는 중복 영향
- Writer Upsert·Unique·Version 정책
- Chunk 재시도 시 중복 안전성
- Skip Item의 별도 원장과 재처리 기준

확인된 Default:

| Property | Default | 제한 |
|---|---:|---|
| `cpf.batch.execution.default-chunk-size` | 100 | 0은 Default, 범위 1~10,000 |
| `cpf.batch.execution.max-partition-count` | 256 | 0은 Default, 범위 1~10,000 |
| `cpf.batch.execution.remote-poll-interval-ms` | 1000 | 0은 Default, 범위 10~60,000 |
| `cpf.batch.execution.remote-timeout-ms` | 3600000 | 0은 Default, 범위 1,000~86,400,000 |
| `cpf.batch.execution.remote-chunk-max-wait-timeouts` | 40 | 0은 Default, 범위 1~10,000 |
| `cpf.batch.execution.remote-chunk-throttle-limit` | 256 | 0은 Default, 범위 1~10,000 |

운영 환경은 Default 사용 여부와 용량 근거를 기록한다.

## 5. JobParameter·Metadata·Checkpoint

`CpfSpringBatchExecutionControl`은 다음 CPF 정보를 JobParameter로 연결한다.

- `cpfExecutionId`, `jobId`, `definitionVersion`
- `approvalId`, `operatorId`, `reason`
- `idempotencyKey`, `fencingToken`
- `planChecksum`, `definitionChecksum`
- `ownerDomain`, `executorReference`, `timeoutSeconds`

Secret, 전체 stdout/stderr, 대용량 Payload를 JobParameter·ExecutionContext에 저장하지 않는다.

## 6. 실행 상태와 제어

| 조치 | 선행 상태 | 결과 | 실패 시 |
|---|---|---|---|
| Start | 승인·정의·Artifact·Fencing 유효 | CPF reserve → Spring JobExecution → Link bind | reserve→start→bind 단계별 대사 |
| Stop | STARTING/STARTED 등 허용 상태 | `JobOperator.stop` accepted | Worker heartbeat·실제 종료 확인 |
| Restart | Restart 가능한 종료·Checkpoint | 새 JobExecution Link | 최신 Fencing·Parameter 호환 확인 |
| Abandon | 재시작 금지 판단 | Metadata abandon | Reason·Approval·Audit |
| Recover | Spring Batch recover 대상 | 복구 JobExecution Link | Runtime 버전·Resource 확인 |
| Reconcile | CPF Execution 존재 | 최신 Spring 상태 Projection | Orphan·Unknown·Link 누락 분류 |

## 7. Idempotency 구현과 검증 기준

현재 Adapter는 `idempotency_scope + idempotency_key`로 Reservation을 조회하고 Canonical `request_hash`, Job/Definition/Approval, Plan Checksum을 비교한다. 같은 Scope·Key와 다른 불변 요청은 `CpfBatchIdempotencyConflictException`으로 거부한다. Duplicate Insert 경합에서도 Winner Reservation의 Hash를 다시 검증한다.

남은 검증은 3 Vendor Unique Constraint, 동시 Start, RESERVED 뒤 Process Kill, Terminal Result Replay, Reconcile Retry다.

## 8. Lease·Claim·Fencing

현재 구현은 `CPF_BATCH_EXECUTION_EPOCH`의 Job별 최신 Fencing Token을 단조 증가시키고, 낮은 Token을 거부하며, Bind Transaction에서 Epoch Row를 잠가 Token 교체와 상태 갱신 경합을 차단한다. 다중 인스턴스·DB 장애·Clock/Lease 경계는 실행 검증이 필요하다.

필수 Scenario:

1. Manager A가 Lease Epoch 10 획득.
2. A 중단 후 B가 Epoch 11 획득.
3. A의 늦은 Write·Completion은 거부.
4. B 결과만 Current로 확정.
5. Lease DB 장애·Clock Skew·Heartbeat 지연을 분류.

## 9. Partition·Remote Worker

- Partition Key·Range·Total Count와 Snapshot 기준을 고정한다.
- Worker는 Claim·Lease·Fencing·Heartbeat·Capability·Version을 보고한다.
- Remote 전달은 Product Profile에서 Kafka 등 공유 Transport를 사용한다.
- Manager 인스턴스 로컬 Reply Queue에 의존하지 않는다.
- Duplicate·Late reply·DLT·Rebalance·Backpressure를 시험한다.

## 10. Scheduler

필수 정보:

- Schedule ID·Job Definition Version
- Cron/Fixed Delay·Timezone·Calendar
- Effective From/To·Enabled
- Misfire 정책: Skip/Fire once/Catch-up 한도
- Cluster Claim·Outbox·Dispatch Attempt
- 승인·Reason·Expected Version·Audit

Trigger Claim과 Job Start 사이 중단은 Outbox 또는 상태 기계로 복구한다.

## 11. Center-Cut

현재 API:

```text
POST /internal/v1/center-cut/executions
POST /internal/v1/center-cut/executions/{jobExecutionId}/stop
POST /internal/v1/center-cut/executions/{jobExecutionId}/restart
POST /internal/v1/center-cut/executions/{jobExecutionId}/abandon
POST /internal/v1/center-cut/executions/{cpfExecutionId}/reconcile
```

Start 입력: `approvalId`, `reason`, `idempotencyKey`, `fencingToken`, `parameters`.

업무 설계:

- 대상 Snapshot·기준 시점
- Partition·Chunk·Commit
- Dry Run·건수 Preview
- Item Claim·Lease·Fencing
- 실패 Item·Unknown Item·failed-only reprocess
- 합계·건수·Hash 대사 기준
- 부분 성공의 운영 표시와 종료 Code

Reconcile Endpoint는 Controller Method에서 Principal·Reason을 직접 받지 않으므로 Security Filter/Method Security가 보호하는지 재확인한다.

## 12. Artifact·Job Pack·Runner·Agent

Artifact Manifest:

- jobId·definitionVersion·Source SHA
- Artifact SHA-256·Signature·Key ID
- Runtime·Java·DB·Capability 요구
- Config Schema·Secret Reference
- Migration Compatibility
- Rollback 대상 Version

Agent는 승인된 Artifact·명령 Allowlist만 실행한다. Process Tree, stdout/stderr 상한, Timeout, Drain, Service User, File Permission, Log Archive, Power loss 후 상태를 확인한다.

## 13. Dry Run·Preview·승인

Dry Run은 실제 Side Effect를 만들지 않고 다음을 반환한다.

- 예상 대상 건수·Partition 수
- Query Plan·Index·Lock 위험
- 예상 Resource·시간 범위
- 제외·중복·Invalid 건수
- 적용 Version·Artifact·Config

실행은 Permission, Reason, Approval, Idempotency, Expected Definition Version, Fencing을 검증한다.

## 14. `UNKNOWN_RESULT`와 대사

Reserve 성공 후 Start 응답 유실, Start 성공 후 Link bind 실패, Worker 처리 후 Reply 유실을 구분한다.

대사 순서:

1. `cpfExecutionId`로 Control 행 조회.
2. Spring Batch `JobExecution`·`JobInstance` 조회.
3. Worker/Partition Ledger 조회.
4. 업무 Item·Result Table 대사.
5. Success·Failed·Stopped·Unknown 확정.
6. Restart·Reprocess·Compensation 결정.
7. Reason·Operator·Approval·Evidence 기록.

## 15. ADM 운영 연결

운영 화면은 Job 정의·Schedule·Instance·Worker Pool·Center-Cut·Agent·Job Pack·Execution·Recovery·Lease·Alert·Audit를 구분한다. Command는 Owner API를 호출하고 DB 직접 Update를 하지 않는다.

## 16. Test

- Job/Step/Tasklet/Chunk Unit·Integration
- Checkpoint·Restart·Stop·Abandon
- Duplicate Parameter·Idempotency Hash Conflict
- Partition 재할당·Stale Worker
- Kafka Duplicate·Late reply·DLT·Broker outage
- Scheduler Misfire·Double trigger
- Agent Kill·Disk full·Artifact checksum failure
- Reserve→Start→Bind 단계별 Response loss
- 3 Vendor Metadata·CPF Control Migration
- Multi-instance Manager·Worker

## 17. EDU: Center-Cut 실행과 복구

1. Job Definition·Version·Checksum 작성.
2. Reader·Processor·Writer와 Commit Interval 구성.
3. Dry Run에서 대상·Partition·Resource 확인.
4. Approval·Reason·Idempotency·Fencing으로 Start.
5. Spring Job/Step ID와 CPF ID 연결 확인.
6. Worker 중단·중복 Reply·Stale token 주입.
7. Stop·Restart·Abandon 허용 조건 확인.
8. Response loss를 주입하고 Reconcile.
9. Failed-only Reprocess와 합계 대사.
10. ADM Audit·Metric·Log·Trace 확인.

## 18. 현재 제한사항

- Build·Spring Batch Runtime 미실행.
- Idempotency Hash·Scope와 최신 Owner Epoch Fencing은 현재 Source에서 확인되지 않음.
- 실제 Kafka·DB·Agent·다중 인스턴스·장애·부하 Evidence 없음.

## 부록 A. 기준 Commit의 Batch Public Contract

| Contract | 주요 Field·제약 | 실행 의미 |
|---|---|---|
| `BatchApprovedLaunchRequest` | Published Definition, Plan, Parameters, Approval ID, Operator, Reason, Idempotency Key, 양수 Fencing Token | 승인된 실행 입력 |
| `BatchExecutionPlan` | Plan ID 1~120자, 양수 Version, Topology, 1개 이상 Step, SHA-256 Checksum | Spring Batch Job Graph 재료 |
| `BatchStepDefinition` | Step ID 1~100자, Executor Type/Reference, Parameter, Partition Count, Success/Failure Transition, Restartable | Step Materialization 계약 |
| `BatchExecutionLink` | CPF Execution ID와 JobInstance/JobExecution/StepExecution ID | Control Plane과 Spring Metadata 연결 |
| `BatchExecutionControlPort` | Start·Stop·Restart·Abandon·Reconcile | 운영 생명주기 Port |

`BatchStepDefinition.partitionCount`는 0 이하를 1로 보정하고 10,000 초과를 거부한다. `BatchExecutionPlan`은 중복 Step ID와 존재하지 않는 Transition Target을 거부한다.

## 부록 B. Runtime Role과 기본 Property

| Runtime | Property | 값 | 의미 |
|---|---|---|---|
| Center-Cut Manager | `spring.batch.job.enabled` | `false` | 기동 시 임의 Job 자동 실행 차단 |
| Center-Cut Manager | `spring.batch.jdbc.initialize-schema` | `never` | 제품 Migration 사용 |
| Center-Cut Manager | `cpf.batch.remote.transport` | `kafka` | Remote 전송 경로 |
| Center-Cut Manager | `cpf.batch.remote.kafka.role` | `MANAGER` | Manager Channel |
| Center-Cut Manager | `cpf.batch.remote.worker-enabled` | `false` | Worker Listener 비활성 |
| Worker | `cpf.batch.remote.kafka.role` | `WORKER` | Worker Channel |
| Worker | `cpf.batch.remote.worker-enabled` | `true` | Worker Listener 활성 |

공통 `cpf.batch.execution` 기본값:

| Property | Default | 제한·운영 영향 |
|---|---:|---|
| `default-chunk-size` | `100` | Item 처리·Commit 단위 |
| `max-partition-count` | `256` | Manager가 허용하는 최대 Partition |
| `remote-poll-interval-ms` | `1000` | Remote 결과 Poll 간격 |
| `remote-timeout-ms` | `3600000` | Remote 전체 Timeout |
| `remote-chunk-max-wait-timeouts` | `40` | 연속 대기 상한 |
| `remote-chunk-throttle-limit` | `256` | Remote Chunk 동시 처리 상한 |

## 부록 C. Center-Cut Control API

Source: `CenterCutControlController`.

| Method·Path | 입력 | 정상 응답 | 운영 확인 |
|---|---|---|---|
| `POST /internal/v1/center-cut/executions` | approvalId, reason, idempotencyKey, fencingToken, parameters | `202` + `BatchExecutionLink` | Operator Principal, Published Definition, Plan Checksum |
| `POST /internal/v1/center-cut/executions/{jobExecutionId}/stop` | reason | `202` + accepted | 실제 Step 종료·Resource 정리 |
| `POST /internal/v1/center-cut/executions/{jobExecutionId}/restart` | reason, fencingToken | `202` + 새 Link | Restart 가능 상태·Checkpoint·최신 소유권 |
| `POST /internal/v1/center-cut/executions/{jobExecutionId}/abandon` | reason | `202` | 재실행 금지 영향·승인 |
| `POST /internal/v1/center-cut/executions/{cpfExecutionId}/reconcile` | Path ID | `200` + 관측 Link | CPF 원장과 Spring Metadata 일치 |

현재 Controller의 Reconcile Endpoint에는 별도 Request Body의 Reason·Approval이 없다. 외부 노출 여부와 Backend Authorization·Audit 적용을 Runtime에서 확인해야 한다.

## 부록 D. Start·Bind 응답 유실과 대사

현재 `CpfSpringBatchExecutionControl.start()` 순서는 다음과 같다.

```text
ledger.reserve
→ fencing.assertCurrent
→ Job materialize
→ JobOperator.start
→ BatchExecutionLink 생성
→ ledger.bind
```

`JobOperator.start` 후 `ledger.bind` 전에 Process가 중단되면 Spring Batch 실행은 존재하지만 CPF Link가 없을 수 있다. 운영 절차:

1. `idempotencyKey`로 CPF Control 행을 조회한다.
2. 동일 Job Parameter의 `cpfExecutionId`로 Spring `JobExecution`을 조회한다.
3. 실행이 존재하면 새 Job을 시작하지 않는다.
4. `reconcile(cpfExecutionId)` 또는 승인된 복구 Command로 Link를 재구성한다.
5. 실행이 없다는 근거가 확인된 경우에만 재실행한다.

## 부록 E. 현재 Idempotency·Fencing 주의사항

### E.1 Idempotency

현재 JDBC Adapter는 `idempotency_key`로 기존 `cpf_execution_id`를 반환한다. Request Hash·Scope·Definition Version·Parameter Hash 충돌을 같은 조회에서 검증하지 않는다. 따라서 같은 Key에 다른 입력이 들어오지 않도록 호출자가 Key Scope를 분리하고, 제품 보강 전에는 기존 Request와 Definition·Parameter·Approval을 운영 대사로 비교한다.

### E.2 Fencing

현재 `assertCurrent`는 `CPF_BATCH_EXECUTION_CONTROL`의 `job_id`, `cpf_execution_id`, `fencing_token`을 비교한다. 최신 Leader Lease Epoch를 별도 조회하는 방식인지 Source 전체를 추가 검증해야 한다. Takeover Test 전에는 Stale Manager 차단을 `미검증`으로 둔다.

## 부록 F. Scheduler Dispatch 상태 흐름

기준 Commit의 Scheduler는 다음 순서를 사용한다.

```text
runtime policy 확인
→ Leader fencing token 확인
→ due schedule 조회
→ available window·business calendar 확인
→ fenced trigger insert
→ 승인 실행 Request Resolve
→ executionControl.start
→ JobExecution ID 연결
→ next_fire_at fenced advance
```

중요 경계:

- Business Calendar Runtime이 중지된 경우 영업일 의존 Schedule을 소진하지 않는다.
- Trigger Insert는 Duplicate Key를 중복 실행 방지 신호로 사용한다.
- Job Start 후 Trigger mark 전 중단은 Trigger·Execution 대사가 필요하다.
- `scheduleId + scheduledAt`를 Idempotency Key로 사용한다.
- `next_fire_at` Update 0건은 동시 변경 또는 Fencing 거부로 처리한다.

Misfire 정책은 Schedule 정의·SQL·ADM 화면과 함께 확인하며, Source에서 확인하지 않은 상태 이름을 문서에 추가하지 않는다.

## 부록 G. Host Agent Property와 배포 판정

| `cpf.agent` Property | Default | 개발·운영 조건 |
|---|---:|---|
| `artifact-repository-base-url` | 없음 | 승인 HTTPS Repository만 등록 |
| `artifact-public-key-path` | 없음 | Legacy 단일 Signature Key |
| `artifact-state-mac-key-base64` | 없음 | Secret Provider로 공급하는 Artifact State MAC Key |
| `artifact-trust-store.*` | 빈 Map | Key Path·NotBefore·NotAfter·Revoked로 Rotation |
| `artifact-allowed-hosts` | 빈 목록 | Repository Host Allowlist |
| `artifact-allowed-content-types` | JAR/octet-stream/ZIP | Response Content-Type 제한 |
| `require-repository-digest-header` | `true` | Repository Digest와 Artifact SHA-256 대조 |
| `allow-private-repository-addresses` | `false` | Private Address 허용은 Network 승인 필요 |
| `artifact-proxy-host/port` | 없음/0 | 승인 Proxy만 사용 |
| `command-ledger-root` | `./data/agent-command-ledger` | 영속 Command Idempotency·결과 원장 |
| `require-signature` | `true` | Signature 검증 해제 금지 |
| `max-artifact-bytes` | `536870912` | 512 MiB |
| `max-log-archive-bytes` | `1073741824` | 1 GiB |
| `process-timeout-seconds` | `60` | Process 제어 Timeout |
| `max-process-output-bytes` | `1048576` | stdout/stderr 1 MiB 상한 |
| `log-archive-ttl-seconds` | `600` | Log Archive 보존 초 |
| `command-ledger-retention-seconds` | `604800` | Command Ledger 7일 기본 |
| `allowed-client-subjects` | 빈 목록 | mTLS Client Subject Allowlist |

Service별 `serviceId`, `artifactId`, `installRoot`, `logRoot`, `systemdUnit`, `windowsStartScript`, `healthUrl`, `runtimeControlUrl`, `runtimeMode`, `environmentCode`, `releaseChannel`을 설정한다. 설치 전 Artifact Hash·Signature·Digest Header·Key 유효기간·Service Identity·경로 권한을 확인한다. 적용 후 Health, Current Pointer, Artifact State MAC, 실제 Build SHA를 대사한다. DNS 검증과 실제 HTTP Connection Address 고정은 `재확인 필요`다.

## 부록 H. Batch 검증 Scenario

| Scenario | 기대 판정 |
|---|---|
| 같은 Idempotency Key·같은 Request | 기존 Execution 조회 또는 동일 결과 Replay |
| 같은 Scope·Key·다른 Parameter/Approval/Definition/Plan | Canonical Request Hash·Plan Checksum 비교로 `CpfBatchIdempotencyConflictException`; 3DB 동시 경합 실행은 `미검증` |
| Manager Start 후 Link 전 중단 | 새 실행 금지, Reconcile |
| Worker 처리 후 Reply 유실 | Item 상태·Spring Metadata·Attempt 대사 |
| Leader 교체 | 과거 Fencing Token Update 거부 |
| Stop 요청 | Step 종료와 Checkpoint·Resource 정리 확인 |
| Restart | 새 JobExecution ID와 기존 JobInstance 관계 확인 |
| Partition 일부 실패 | Failed Partition만 재처리하고 전체 건수 대사 |
| Agent Artifact 검증 실패 | 설치·기동 차단, 기존 Artifact 유지 |
| Scheduler Trigger 중복 | Unique/Fencing으로 단일 Start |

## 부록 I. Primary Engine·Job 등록·진행 상태

Spring Batch는 Job·Step·JobRepository·ExecutionContext·Checkpoint·Restart의 **Primary Engine**이다. CPF Job 등록은 Published Definition, Definition Version, Execution Plan Version, SHA-256 Checksum, Owner Domain, Executor Reference를 묶어 수행한다. 같은 Job ID의 새 정의는 기존 Version을 덮어쓰지 않고 새 Version으로 등록하며, 실행 중에는 Definition·Plan·Artifact Version을 고정한다.

진행 상태는 CPF Execution ID와 Spring JobExecution·StepExecution을 연결해 Read·Write·Skip·Error Count, Partition·Worker, Checkpoint, Last Update, Exit Status를 조회한다. ADM 진행률이 Spring Metadata와 다르면 Reconcile 전까지 확정 상태로 표시하지 않는다.

---

## 기준 Source와 역할 완결성 판정

- Repository: `https://github.com/freeangelsun/202412_01_CPF`
- Branch: `master`
- 기준 Commit: `c2e1680fcf42467d445df97f1a3a0c36dab783ef`
- 문서 표준: `cpf-docs/specification/CPF_DOCUMENTATION_STANDARD.md`
- 제품 목표 정본: `cpf-docs/governance/CPF_FINAL_TARGET_REQUIREMENTS.md`
- 사실 우선순위: 실제 Source·SQL·API·Config·Frontend·Script·Test → Architecture·Specification → 이 매뉴얼

이 매뉴얼의 대상 역할은 다음 흐름을 다른 사람의 구두 설명이나 Source 역분석 없이 수행할 수 있어야 한다.

```text
업무 목적 파악
→ 선행 조건과 권한 준비
→ 실제 기능 위치 탐색
→ 등록·설정·개발·실행
→ 상태와 결과 확인
→ 오류·동시성·부분 실패 판단
→ Retry·Restart·Reprocess·Reconcile·Compensation·Rollback
→ Log·Metric·Trace·Audit·Evidence 확인
→ 정상화와 완료 판정
```

Source에 구현되지 않았거나 현재 Gate가 실패한 기능은 사용 가능한 기능처럼 설명하지 않는다. 해당 단락의 상태를 `미구현`, `미검증`, `실패`, `재확인 필요`로 표시하고 실행 중단 조건과 확인 경로를 함께 제공한다.

---

## 제2부 실무편: Spring Batch 기능 전수 개발·운영 연결

## 19. Batch 제품 구성과 실행 책임

| Module | 책임 | 실제 Consumer | 상태 |
|---|---|---|---|
| `cpf-batch/contract` | Job Definition, Execution Plan, Step, 승인 실행 요청, Control State, Port·SPI | Control Server, Scheduler, Execution Runtime, ADM | `부분 구현` |
| `runtime-common` | Runtime 정책·공통 상태 | Scheduler, Worker, Agent | `부분 구현` |
| `execution-runtime` | Spring Batch Job materialize, JobOperator 제어, 실행·Remote Message Ledger | Scheduler, Center-Cut, Worker | `부분 구현`, Kafka/DB Runtime `미검증` |
| `control-server` | Definition·Deployment·Cell·Recovery Control Plane | ADM Backend | `부분 구현`, Multi-instance `미검증` |
| `scheduler` | Due Claim, Calendar, Durable Launch Outbox, Fencing | Batch Control Runtime | `부분 구현`, Process Kill `미검증` |
| `worker` | Tasklet/Chunk/Protocol Adapter 실행 | Remote Manager | `부분 구현`, Outbound Policy 부족 |
| `center-cut-runner` | 승인된 Center-Cut 실행·Stop·Restart·Abandon·Reconcile API | ADM/운영자 | `부분 구현` |
| `host-agent` | Artifact 검증·설치·Process·Health·Command Ledger | Deployment Engine | `부분 구현`, DNS/TLS Identity `재확인 필요` |
| `testkit` | Batch Contract·Fault Test 지원 | Batch 개발자 | `부분 구현` |

Spring Batch의 `JobInstance`, `JobExecution`, `StepExecution`, `ExecutionContext`가 실행 엔진 정본이다. CPF Control Plane은 승인·정의 Version·Idempotency·Fencing·Audit·Deployment·Reconciliation을 추가하며 별도 사설 실행 엔진을 만들지 않는다.

## 20. Job Definition·Plan·Step 계약

### 20.1 승인 실행 요청

`BatchApprovedLaunchRequest`의 필수 입력:

| 입력 | 제약 |
|---|---|
| `definition` | 상태 `PUBLISHED`, Checksum SHA-256 |
| `plan` | Definition의 Job ID·Version과 일치, Integrity 검증 |
| `parameters` | Canonical immutable 값으로 정규화 |
| `approvalId` | 1~120자 |
| `operatorId` | 1~120자 |
| `reason` | 5~500자 |
| `idempotencyKey` | 8~200자, `[A-Za-z0-9._:-]` |
| `fencingToken` | 양수 |

Idempotency Scope는 `jobId:definitionVersion:approvalId`이고 Request Hash는 Definition·Plan·Parameter·승인 범위를 Canonical Digest로 계산한다. 같은 Scope/Key에 다른 Hash가 오면 재실행하지 않고 충돌로 처리한다.

### 20.2 Execution Plan

- `planId`는 Job ID와 일치한다.
- `planVersion`은 Definition Version과 일치한다.
- Step은 1개 이상이며 `stepId`가 중복되면 안 된다.
- Success/Failure Transition Target은 등록된 Step 또는 `END`여야 한다.
- Plan Checksum은 SHA-256이다.
- Plan을 저장·전송한 뒤 실행 시점에 다시 Integrity를 검증한다.

### 20.3 Step Definition

| 항목 | 의미·검증 |
|---|---|
| `stepId` | 1~100자 식별자 |
| `executorType` | Tasklet·Chunk·Remote·Protocol 등 Definition Enum |
| `executorReference` | 실제 Registry/Bean/Handler 식별자 |
| `parameters` | Step 고정 Parameter |
| `partitionCount` | 기본 1, 제품 상한 10,000; 운영 상한은 `maxPartitionCount` |
| `nextOnSuccess`, `nextOnFailure` | Flow Transition |
| `restartable` | 실패 뒤 Restart 허용 여부 |

Definition이 허용하더라도 Runtime Property 상한, Worker Pool Capacity, DB Connection, Kafka Partition 수를 넘으면 실행을 거부하거나 Preview에서 차단한다.

## 21. Tasklet·Chunk·Reader·Processor·Writer

### 21.1 Tasklet

Tasklet은 하나의 명확한 Side Effect 또는 짧은 Control 작업에 사용한다.

1. Input Parameter와 Restart 의미를 정의한다.
2. 외부 Side Effect 전 멱등 원장을 기록한다.
3. ExitStatus와 BatchStatus를 혼동하지 않는다.
4. Process Kill 뒤 재호출 가능한지, 결과 조회가 필요한지 명시한다.
5. Long-running Loop는 Chunk/Partition 또는 별도 Worker로 전환한다.

### 21.2 Chunk

```text
Reader가 Commit 구간만큼 읽음
→ Processor가 업무 Validation/변환
→ Writer가 Side Effect 반영
→ Transaction Commit
→ ExecutionContext Checkpoint 저장
```

- `defaultChunkSize` 기본 100, 범위 1~10,000.
- Item 자체 식별자와 Chunk Sequence를 보존한다.
- Skip은 데이터 오류에만 제한하고 시스템 오류를 무시하는 수단으로 사용하지 않는다.
- Retryable Exception, Retry 횟수, Backoff와 최종 실패 상태를 정의한다.
- Writer가 외부 API를 호출하면 Item별 Idempotency Key와 Result 조회가 필요하다.

### 21.3 Reader·Processor·Writer 선택

| 유형 | Reader | Writer | 핵심 검증 |
|---|---|---|---|
| DB→DB | Paging/Cursor | Batch JDBC/JPA | 정렬 Key, Isolation, 중복·누락 |
| File Import | Streaming CSV/XLSX | Domain Command | Checksum, Row 번호, Dry Run, Partial |
| API Job | DB/Queue | Protocol Adapter | SSRF/TLS/Timeout/Result Unknown |
| Export | DB Paging | Streaming File/Archive | Masking, Disk, Checksum, Retention |
| Center-Cut | Target Provider | Result Ledger | Partition/Chunk/Commit/대사 기준 |

## 22. JobParameter·Identity·Metadata·Checkpoint

### Job Identity

- 식별 Parameter와 비식별 Parameter를 구분한다.
- CPF는 `cpfExecutionId`, `jobId`, `definitionVersion`, `approvalId`, `idempotencyKey`, `fencingToken`, Plan/Definition Checksum을 연결한다.
- Business Date, File Checksum, Schedule ID 등 재실행 의미에 영향을 주는 값은 식별 여부를 명시한다.

### Metadata

- Spring Batch Metadata Schema는 `spring.batch.jdbc.initialize-schema=never`; 운영자가 Vendor별 Migration으로 설치한다.
- CPF Control Ledger는 Spring IDs와 CPF 실행 ID를 연결한다.
- Metadata나 Link Row를 수동 UPDATE해 상태를 맞추지 않는다.

### Checkpoint

- Reader Position과 Writer Side Effect 경계를 함께 검증한다.
- Commit 직후 Process Kill Scenario에서 이미 반영한 Item이 재처리되는지 확인한다.
- ExecutionContext에 Secret·대용량 Payload·직렬화 불가능 객체를 저장하지 않는다.

## 23. 실행 상태·Stop·Restart·Abandon·Recover

CPF Control State:

```text
RESERVED → STARTING → STARTED → COMPLETED
                         ├→ STOPPING → STOPPED
                         ├→ FAILED
                         └→ UNKNOWN_RESULT
FAILED/STOPPED → Restart 조건 검증 → STARTING
복구 불가 실행 → ABANDONED
검증·승인 거부 → REJECTED
```

`UNKNOWN_RESULT`는 Terminal이 아니다. Spring Batch Metadata, CPF Link, Scheduler Outbox, Worker Message Ledger, 외부 Side Effect를 대사해 상태를 갱신한다.

Center-Cut Control API:

| Method·Path | 입력 | 정상 응답 | 실패 처리 |
|---|---|---|---|
| `POST /internal/v1/center-cut/executions` | approvalId, reason, idempotencyKey, fencingToken, parameters | 202 + `BatchExecutionLink` | 응답 유실 시 Key/CPF Execution ID 조회 |
| `POST .../{jobExecutionId}/stop` | reason | 202 accepted | Spring 상태 재조회 |
| `POST .../{jobExecutionId}/restart` | reason, fencingToken | Link | Stale Token 거부 |
| `POST .../{jobExecutionId}/abandon` | reason | 202 | 재시작 불가 영향 확인 |
| `POST .../{cpfExecutionId}/reconcile` | CPF Execution ID | 최신 Link | 미존재·Link 불일치 조사 |

운영 API가 `recover`를 직접 노출하지 않는 경우 내부 Method가 있다는 이유로 화면에서 사용할 수 있다고 설명하지 않는다.

## 24. Idempotency·Request Hash·Reservation

```text
승인 실행 요청 정규화
→ idempotencyScope + key 조회
→ 미존재: requestHash와 RESERVED 기록
→ 같은 Hash: 기존 Reservation/Result 반환
→ 다른 Hash: CpfBatchIdempotencyConflictException
→ STARTING 기록 후 JobOperator.start
→ Spring ID Bind
```

필수 Fault Test:

1. 두 Thread가 같은 Key·Hash로 동시에 Start.
2. 같은 Key·다른 Parameter/Approval/Definition.
3. RESERVED 뒤 Process Kill.
4. JobOperator Start 성공 뒤 Link Bind 실패.
5. Link Bind 성공 뒤 HTTP 응답 유실.
6. Restart와 신규 Start가 동시에 경합.

정상 판정은 실행이 하나라는 사실만이 아니라, 두 호출자가 동일 Reservation/Link를 받거나 명확한 충돌을 받는 것이다.

## 25. Lease·Claim·Fencing

- Scheduler Leader Lease, Trigger Claim, Worker Claim, Deployment Cell Lock은 소유 Resource가 다르다.
- Fencing Token은 Lease 갱신 시 증가하고 모든 상태 변경 SQL Predicate에 포함돼야 한다.
- 기준 Commit은 `CPF_BATCH_EXECUTION_EPOCH`의 Job별 최신 Token을 조회하고 Bind Transaction에서 Epoch Row를 잠근다. Vendor별 `FOR UPDATE`·동시 갱신 의미와 다중 인스턴스 실행은 `미검증`이다.
- Lease 만료 뒤 과거 Worker가 완료 응답을 보내면 `late reply`로 분리하고 현재 상태를 덮어쓰지 않는다.

## 26. Partition·Remote Partition·Remote Chunk

### Local Partition

- Partition Key 범위가 겹치거나 빠지지 않는지 Preview한다.
- `maxPartitionCount` 기본 256, 범위 1~10,000.
- Grid Size와 Executor Core/Max/Queue를 함께 조정한다.

### Remote

기본 Kafka Contract:

| Property | Default·범위 |
|---|---|
| `cpf.batch.remote.kafka.request-topic` | `cpf.batch.remote.requests.v2` |
| `reply-topic-prefix` | `cpf.batch.remote.replies.v2` |
| `consumer-group` | `cpf-batch-remote-workers-v2` |
| `role` | `ALL`; Manager/Worker 별 분리 권장 |
| `send-timeout` | 30초 |
| `message-ttl` | 5분, 최대 1시간 |
| `max-envelope-bytes` | 2 MiB |
| `max-payload-bytes` | 1 MiB, Envelope 이하 |
| `max-headers` | 32 |
| `max-header-value-length` | 512 |
| `max-delivery-attempts` | 5 |
| `max-payload-depth` | 32, 최대 128 |
| `reply-queue-capacity` | 1,000, 최대 100,000 |

`managerInstanceId`는 명시 Property, `CPF_INSTANCE_ID`, `HOSTNAME` 중 하나가 필요하다. 허용 Producer ID, Environment, Tenant, Message TTL·Size·Depth를 Codec과 Ledger 양쪽에서 검증한다.

Remote Message Ledger에서 Request/Reply Message ID, Producer, Attempt, Lease, Status, Payload Hash를 확인한다. Broker ACK, Worker Side Effect, Reply Publish 중 어느 단계에서 끊겼는지 구분하지 못하면 `UNKNOWN_RESULT`다.

## 27. Scheduler·Calendar·Misfire·Durable Launch Outbox

### 정상 Dispatch

```text
Leader Lease 확인
→ Due Schedule 조회
→ 영업시간·Calendar 판정
→ Fenced Trigger Claim/Insert
→ Durable Launch Outbox 기록
→ 승인 실행 요청 생성
→ Job Start/Link Bind
→ Trigger Dispatched 표시
→ next_fire_at CAS Advance
```

- 영업일 전용 Schedule에서 Calendar Runtime이 중지되면 `next_fire_at`을 소진하지 않는다.
- `NEXT_BUSINESS_DAY`와 `SKIP` 정책을 구분한다.
- `next_fire_at` CAS 또는 Fencing 실패는 다른 Leader가 처리한 것으로 재조회한다.
- Trigger Claim 뒤 Process Kill이면 Outbox가 재개해야 하며 신규 Trigger를 만들지 않는다.
- Misfire 정책은 각 Schedule 정의의 허용 값, 횟수 상한, 과거 실행 Catch-up 여부를 문서화한다.

## 28. Center-Cut

Center-Cut Definition에는 다음을 명시한다.

- 대상 Provider와 대상 Selection 조건.
- Business Key·Business Date.
- Partition 수와 범위.
- Chunk Size와 Commit 기준.
- Handler Reference.
- Retry Limit과 Retryable Error.
- Result Status, Payload Masking, 대사 기준.
- 중복 Target·이미 처리된 Target 정책.

ADM `batch` 화면에서 Job, Target, Result를 교차 조회한다. Target의 `transactionId`, Parent Segment, Segment ID와 Result ID를 보존한다. 실패 건수만 보고 재수행하지 않고 Row별 Side Effect와 Result Ledger를 확인한다.

## 29. Job Pack·Artifact·Runner·Worker·Agent

### Artifact

1. Artifact ID·Version·SHA-256·Signature·Manifest를 고정한다.
2. Repository URL·TLS·Host/IP Identity를 확인한다.
3. Download Size 상한, Temp Directory, Atomic Install/Current Pointer를 사용한다.
4. 설치 뒤 Health가 실패하면 이전 Version으로 Rollback한다.

### Host Agent 주요 Property

- Repository URL, Allowed Host·Content Type, Proxy, Private Address 허용 정책.
- Legacy Public Key와 Versioned Trust Store의 Key Path·유효기간·폐기 상태.
- Signature, Repository Digest Header, Artifact State MAC Key.
- Artifact/Log/Process Output Size와 Process Timeout.
- Command Ledger Root·Retention과 Client Subject Allowlist.
- Service별 installRoot, logRoot, systemdUnit/Windows Script, healthUrl, runtimeControlUrl, runtimeMode, environment, release channel.

QA34에서 Agent DNS 검사와 실제 HTTP 연결 Address가 고정되지 않는 문제가 `재확인 필요`다. Redirect, Proxy, DNS 변경, 혼합 A/AAAA, Metadata Address Fixture가 통과하기 전 신뢰되지 않은 Artifact Source를 등록하지 않는다.

## 30. Deployment·Cell·Partial Failure·Reconciliation

```text
Manifest 검증
→ Plan 생성
→ BAT/DEPLOY_PLAN 승인
→ Cell Lock 획득
→ Instance별 Artifact 설치
→ Process 전환
→ Health Probe
→ 결과 집계
→ SUCCESS / PARTIAL_FAILED / UNKNOWN_RESULT
→ Reconcile 또는 Compensation/Rollback
```

- 같은 Operation ID와 다른 Request Hash는 충돌이다.
- Cell Lock을 잃은 실행은 이후 Side Effect를 중단한다.
- Instance별 단계와 오류를 저장한다.
- 일부 Instance만 변경된 상태에서 전체 성공으로 표시하지 않는다.
- Reconciliation은 실제 Agent State·Artifact Pointer·Health를 다시 읽어 Terminal 상태를 결정한다.
- Rollback은 이전 Artifact/Config/DB 호환성을 확인하고 별도 승인·사유로 실행한다.

## 31. ADM Batch 화면 사용 연결

### `batch`

Job 등록·수동 실행·실패 재수행·중지·Scheduler 1회 실행·Job 상세·Schedule Simulation·Relation·Target·Step·Worker·Lock·Ghost·Operation Log·CSV를 제공한다.

필수 입력: Job ID/명/유형, Execution/Schedule ID, Parameter, Calendar, Business Date, Dispatch Status, Heartbeat Timeout, Lock Key, Ghost Action, Reason.

### Batch Runtime Views

`batch-overview`, `batch-runtime`, `batch-instances`, `batch-scheduler`, `batch-worker-pools`, `batch-center-cut`, `batch-agents`, `batch-job-packs`, `batch-executions`, `batch-recovery`, `batch-leases`, `batch-alerts`, `batch-audit`, `workers`는 Control Server View를 조회한다. 응답의 `stale=true` 또는 `partial=true`를 빈 결과/정상으로 해석하지 않는다.

### Deployment

Manifest JSON과 Reason으로 Plan을 생성한 뒤 Approval 화면에서 `BAT / DEPLOY_PLAN` 또는 `ROLLBACK_PLAN`을 승인한다. Plan 생성은 배포 완료가 아니다.

## 32. Batch Test·Fault Injection Matrix

| Scenario | 주입 위치 | 확인 상태 | 복구 |
|---|---|---|---|
| Step 중간 실패 | Processor/Writer | FAILED, Checkpoint | Restart 가능 여부와 중복 확인 |
| Commit 직후 Kill | DB Commit 후 Metadata 전 | Unknown/재처리 위험 | 업무 Key·DB·Metadata 대사 |
| 같은 Job 중복 실행 | Start 동시 요청 | 같은 Reservation 또는 Conflict | 신규 Key 생성 금지 |
| Worker 응답 유실 | Side Effect 후 Reply 전 | Message Ledger Unknown | Worker/업무 DB 조회 |
| Lease 만료 | 실행 중 Clock/DB | Stale Token 거부 | 새 Owner만 진행 |
| Scheduler Trigger 뒤 Kill | Claim/Outbox 사이 | Pending Outbox | 같은 Trigger 재개 |
| Deployment 일부 실패 | Instance N Health 실패 | PARTIAL_FAILED | Reconcile/Compensation/Rollback |
| Artifact DNS 변경 | 검증 뒤 연결 전 | Security Reject 기대 | Source 격리 |

## 33. Batch EDU

### EDU-BAT-01 Tasklet

Definition Draft→Validate→Publish→Approval→Start→ADM Execution 확인→의도적 Exception→Restart→Audit Evidence를 수행한다.

### EDU-BAT-02 Chunk

1. 1,000개 Item과 Chunk 100을 준비한다.
2. 450번째 Item에서 실패를 주입한다.
3. Commit된 Item과 Checkpoint를 확인한다.
4. 오류를 제거하고 Restart한다.
5. 중복 Side Effect가 없는지 업무 Key로 검증한다.

### EDU-BAT-03 Remote Worker

Manager/Worker Role, Topic, Producer Allowlist, TTL/Size를 설정한다. Worker Side Effect 후 Reply 직전 Process를 종료하고 Message Ledger와 업무 DB를 대사해 Blind Retry 없이 결과를 확정한다.

### EDU-BAT-04 Center-Cut

Target Preview→승인→실행→Target/Result 조회→부분 실패→Row 대사→선택 재처리→전체 건수와 업무 합계 대사를 수행한다.

## 34. Batch 완료 Checklist

- [ ] Definition·Plan·Step Checksum과 Version
- [ ] JobParameter Identity와 Restart 의미
- [ ] Chunk/Commit/Checkpoint와 중복 정책
- [ ] Stop·Restart·Abandon·Unknown 상태
- [ ] Idempotency Scope·Key·Request Hash
- [ ] Lease·Claim·Fencing·Late Reply
- [ ] Partition 범위·Capacity
- [ ] Kafka Envelope·Ledger·DLT
- [ ] Scheduler Calendar·Misfire·Outbox
- [ ] Center-Cut Target·Result·대사
- [ ] Artifact Signature·Install·Health·Rollback
- [ ] Deployment Cell·Partial·Reconcile
- [ ] Permission·Approval·Reason·Audit
- [ ] ADM 조회와 조치 활성 조건
- [ ] Unit·DB·Kafka·Multi-instance·Fault Evidence

---
## 부록 J. Batch Source·SQL 진입점

| 기능 | 기준 Source·SQL |
|---|---|
| 승인 실행 요청 | `cpf-batch/contract/src/main/java/com/cpf/batch/api/BatchApprovedLaunchRequest.java` |
| 실행 계획 | `cpf-batch/contract/src/main/java/com/cpf/batch/api/BatchExecutionPlan.java` |
| Step 정의 | `cpf-batch/contract/src/main/java/com/cpf/batch/api/BatchStepDefinition.java` |
| Control 상태 | `cpf-batch/contract/src/main/java/com/cpf/batch/api/BatchControlState.java` |
| Spring Batch 제어 | `cpf-batch/execution-runtime/src/main/java/com/cpf/batch/execution/CpfSpringBatchExecutionControl.java` |
| 실행 Reservation·Epoch·Link | `cpf-batch/execution-runtime/src/main/java/com/cpf/batch/execution/JdbcBatchExecutionControlPlaneAdapter.java` |
| 실행 Property | `cpf-batch/execution-runtime/src/main/java/com/cpf/batch/execution/CpfBatchExecutionProperties.java` |
| Remote Kafka Property | `cpf-batch/execution-runtime/src/main/java/com/cpf/batch/execution/CpfBatchKafkaRemoteProperties.java` |
| Remote Codec | `cpf-batch/execution-runtime/src/main/java/com/cpf/batch/execution/CpfBatchRemoteCodec.java` |
| Remote Message Ledger | `cpf-batch/execution-runtime/src/main/java/com/cpf/batch/execution/JdbcCpfBatchRemoteMessageLedger.java` |
| Scheduler Dispatch | `cpf-batch/scheduler/src/main/java/com/cpf/batch/scheduler/SchedulerDispatchService.java` |
| Center-Cut API | `cpf-batch/center-cut-runner/src/main/java/com/cpf/batch/centercut/runner/CenterCutControlController.java` |
| Deployment State Machine | `cpf-batch/control-server/src/main/java/com/cpf/batch/control/deploy/DeploymentEngine.java` |
| Deployment Reconcile API | `cpf-batch/control-server/src/main/java/com/cpf/batch/control/deploy/DeploymentRecoveryController.java` |
| Worker Executor Registry | `cpf-batch/worker/src/main/java/com/cpf/batch/worker/BatchRuntimeExecutorRegistry.java` |
| Agent Property | `cpf-batch/host-agent/src/main/java/com/cpf/batch/agent/AgentProperties.java` |
| Artifact 설치 | `cpf-batch/host-agent/src/main/java/com/cpf/batch/agent/internal/ArtifactInstaller.java` |
| DB Migration | `cpf-tools/db/vendor/{mariadb,postgresql,oracle}/migration/**/V87__batch_remote_message_ledger.sql`~`V90__deployment_request_hash_reconciliation.sql` |
| Runtime SQL | `cpf-tools/db/vendor/{mariadb,postgresql,oracle}/runtime/bat/repository` |
