$r=(Resolve-Path .).Path; $z=Join-Path $r 'CPF_DEVGPT_SESSION17_FINAL.zip'; if((git rev-parse HEAD).Trim() -ne '9f16468cccae71523f65f0aefcd94322788c4dd0'){throw 'Target SHA mismatch'}; if(!(Test-Path "$z.sha256")){throw 'Missing ZIP sha256 sidecar'}; $expected=((Get-Content "$z.sha256" -Raw).Trim() -split '\s+')[0].ToLower(); $actual=(Get-FileHash $z -Algorithm SHA256).Hash.ToLower(); if($actual -ne $expected){throw 'ZIP SHA-256 mismatch'}; $tmp=Join-Path ([IO.Path]::GetTempPath()) ('cpf17-'+[Guid]::NewGuid().ToString('N')); try{Expand-Archive -Force $z $tmp; Get-ChildItem $tmp -Recurse -File | ForEach-Object{$rel=$_.FullName.Substring($tmp.Length+1);$dst=Join-Path $r $rel;New-Item -ItemType Directory -Force (Split-Path $dst)|Out-Null;Copy-Item -Force $_.FullName $dst}} finally{if(Test-Path $tmp){Remove-Item -Recurse -Force $tmp}}; Write-Host 'Overlay applied; no repository delete/commit/push performed.'
