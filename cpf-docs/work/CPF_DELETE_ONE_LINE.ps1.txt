$ErrorActionPreference='Stop'; $r=(git rev-parse --show-toplevel).Trim(); $m=Join-Path $r 'cpf-docs/work/CPF_DELETE_MANIFEST.csv'; if(!(Test-Path -LiteralPath $m)){throw "Missing delete manifest: $m"}; $prot=@('cpf-docs/deliverables/','cpf-docs/guides/','cpf-docs/assets/manuals/','cpf-docs/assets/readme/','cpf-docs/specification/CPF_DOCUMENTATION_STANDARD.md','cpf-docs/environment/docker/','cpf-tools/environment/docker-development-test/'); $rows=@(Import-Csv -LiteralPath $m); $hist=Join-Path $r 'cpf-docs/work/v9i'; if(Test-Path -LiteralPath $hist){$known=@{}; $rows|ForEach-Object{$known[$_.path]=$true}; $extra=@(Get-ChildItem -LiteralPath $hist -Recurse -File -Force|ForEach-Object{$rel=$_.FullName.Substring($r.Length).TrimStart('\','/').Replace('\','/'); if(!$known.ContainsKey($rel)){[pscustomobject]@{path=$rel;reason='Historical V9I file discovered during exact cleanup inventory';replacement_path='cpf-docs/work/REVIEW_INDEX.md';owner='Documentation Governance';requirement_id='NXT-HYG-006';protected_check='PASS';delete_status='APPROVED_BY_USER_REQUEST_IF_PRESENT'}}}); if($extra.Count-gt0){$rows=@($rows+$extra|Sort-Object path -Unique); $rows|Export-Csv -LiteralPath $m -NoTypeInformation -Encoding utf8}}; $deleted=0; foreach($x in @($rows)){ $rel=[string]$x.path; if([string]::IsNullOrWhiteSpace($rel)-or$rel.Contains('*')-or$rel.Contains('?')-or$rel.Split('/')-contains'..'-or[IO.Path]::IsPathRooted($rel)){throw "Unsafe delete path: $rel"}; $n=$rel.Replace('\','/'); foreach($p in $prot){if($n.StartsWith($p,[StringComparison]::OrdinalIgnoreCase)){throw "Protected delete blocked: $rel"}}; if($x.replacement_path){$rp=Join-Path $r ([string]$x.replacement_path); if(!(Test-Path -LiteralPath $rp)){throw "Replacement missing for $rel : $($x.replacement_path)"}}; $full=Join-Path $r $rel; if(Test-Path -LiteralPath $full -PathType Leaf){Remove-Item -LiteralPath $full -Force; $deleted++}}; if(Test-Path -LiteralPath $hist){Get-ChildItem -LiteralPath $hist -Recurse -Directory -Force|Sort-Object {$_.FullName.Length} -Descending|ForEach-Object{if(@(Get-ChildItem -LiteralPath $_.FullName -Force).Count-eq0){Remove-Item -LiteralPath $_.FullName -Force}}; if((Test-Path -LiteralPath $hist)-and@(Get-ChildItem -LiteralPath $hist -Force).Count-eq0){Remove-Item -LiteralPath $hist -Force}}; Write-Host "CPF exact cleanup complete. deleted_files=$deleted"; git -C $r status --short
