$ErrorActionPreference='Stop'; $r=(git rev-parse --show-toplevel).Trim(); $f=Join-Path $r 'cpf-docs/governance/CPF_FINAL_TARGET_REQUIREMENTS.md'; $t=Get-Content -LiteralPath $f -Raw; $i=$t.IndexOf('## 23. Legacy Alias Mapping'); if($i-lt0){throw 'Legacy Alias section missing'}; $active=$t.Substring(0,$i); $n=[regex]::Matches($active,'(?m)^\|\s*`[A-Z0-9-]+`\s*\|').Count; if($n-ne186){throw "Canonical count mismatch: $n"}; $s=@(Import-Csv (Join-Path $r 'cpf-docs/work/REQUIREMENT_STATUS.csv')); $nxt=@($s|Where-Object{$_.exact_id -like 'NXT-*'}).Count; if($nxt-ne47){throw "NXT status count mismatch: $nxt"}; if(Test-Path -LiteralPath (Join-Path $r 'cpf-docs/work/v9i')){throw 'Historical v9i workspace still exists; run CPF_DELETE_ONE_LINE first'}; Get-Content (Join-Path $r 'cpf-tools/generator/contracts/cpf-starter-catalog.json') -Raw|ConvertFrom-Json|Out-Null; git -C $r diff --check; Write-Host "CPF currentization verification PASS. canonical=$n nxt=$nxt"; git -C $r status --short
