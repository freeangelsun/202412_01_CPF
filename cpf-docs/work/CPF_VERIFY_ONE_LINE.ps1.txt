$r=(git rev-parse --show-toplevel).Trim(); $bad=@(); Import-Csv (Join-Path $r 'cpf-docs/work/CPF_DELETE_MANIFEST.csv') | ForEach-Object { if(Test-Path -LiteralPath (Join-Path $r $_.path)){$bad+=$_.path}; if(!(Test-Path -LiteralPath (Join-Path $r $_.replacement_path))){$bad+="MISSING_REPLACEMENT:$($_.replacement_path)"} }; if($bad.Count){$bad; throw "Hygiene verification failed"}; git diff --check; git status --short
