& { $root=(git rev-parse --show-toplevel).Trim(); if($LASTEXITCODE -ne 0 -or [string]::IsNullOrWhiteSpace($root)){throw 'Git Repository Root를 확인할 수 없습니다.'}; $old=Join-Path $root 'cpf-docs/work/current/CPF_DEVGPT_CONTROL_V9/_session_workspace/CPF-V9-FINAL-INTEGRATION-CLOSURE-20260806/REV-001/sessions/DEVGPT-V9-INTEGRATION'; $new=Join-Path $root 'cpf-docs/work/v9i'; if(!(Test-Path -LiteralPath $new)){throw '짧은 정본 cpf-docs/work/v9i가 없습니다.'}; if(!(Test-Path -LiteralPath (Join-Path $new 'results/REQUIREMENT_STATUS_INDEX.csv')) -or !(Test-Path -LiteralPath (Join-Path $new 'evidence/FINAL_INTEGRITY.json'))){throw '짧은 정본의 핵심 원장 또는 Evidence가 없습니다.'}; if(!(Test-Path -LiteralPath $old)){Write-Host '삭제 대상 없음: 긴 중복 Workspace가 이미 없습니다.'; return}; $sourceExt=@('.java','.kt','.groovy','.gradle','.sql','.ts','.tsx','.js','.jsx','.vue','.xml','.yml','.yaml','.properties','.ps1','.sh','.bat','.cmd'); $product=Get-ChildItem -LiteralPath $old -Recurse -File | Where-Object { $sourceExt -contains $_.Extension.ToLowerInvariant() }; if($product){throw ('세션 문서 Root 안에서 Product Source 형식이 발견되어 중단: '+(($product.FullName)-join '; '))}; $skip=@('APPLY_ONE_LINE.txt','LOW_COST_VERIFY_ONE_LINE.txt','PACKAGE_MANIFEST.json','SHA256SUMS.txt','evidence/FINAL_INTEGRITY.json','results/REVIEW_FILE_INVENTORY.csv'); $bad=@(); Get-ChildItem -LiteralPath $old -Recurse -File | ForEach-Object { $rel=$_.FullName.Substring($old.Length).TrimStart([IO.Path]::DirectorySeparatorChar,[IO.Path]::AltDirectorySeparatorChar); $norm=$rel -replace '\\','/'; $peer=Join-Path $new $rel; if(!(Test-Path -LiteralPath $peer)){$bad+=('MISSING:'+ $norm)} elseif($skip -notcontains $norm){if((Get-FileHash -Algorithm SHA256 -LiteralPath $_.FullName).Hash -ne (Get-FileHash -Algorithm SHA256 -LiteralPath $peer).Hash){$bad+=('HASH:'+ $norm)}} }; if($bad.Count -gt 0){throw ('짧은 정본과 일치하지 않아 삭제 중단: '+($bad -join '; '))}; Remove-Item -LiteralPath $old -Recurse -Force; Write-Host '삭제 완료: 긴 중복 통합 세션 문서 Root만 제거했습니다.' }
