# EDU/EDU 공통 실행 설정입니다.

cpf:
  framework:
    module-id: ${EDU_MODULE_ID:EDU}
    was-id: ${EDU_WAS_ID:${WAS_ID:eduAP01}}
  service-call:
    registry-mode: ${EDU_SERVICE_REGISTRY_MODE:${CPF_SERVICE_REGISTRY_MODE:LOCAL}}

logging:
  config: classpath:log/cpf-logback-spring.xml
