# 자동화 Test는 ephemeral port를 사용합니다. 실제 외부기관 연결을 구성하지 않습니다.
server:
  port: 0
cpf:
  integration:
    domain-call:
      bindings:
        MBR:
          # 동일 JVM operation이 있으면 LOCAL, 없으면 Registry 기반 REMOTE를 선택합니다.
          mode: AUTO
          service-id: MBR
