server:
  # Test Harness만 ephemeral port를 허용합니다.
  port: 0
cpf:
  openapi:
    webmvc:
      api-docs-enabled: ${CPF_OPENAPI_WEBMVC_API_DOCS_ENABLED:true}
  gateway:
    environment-code: test
    require-distributed-rate-limit-counter: false
    rate-limit-counter-mode: LOCAL
    require-tls-ingress: false
