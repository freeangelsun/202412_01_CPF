# XYZ/EDU local profile 설정입니다.

server:
  port: ${XYZ_SERVER_PORT:8099}

spring:
  datasource:
    xyz:
      url: ${XYZ_DATASOURCE_URL:${XYZ_DB_URL:jdbc:mariadb://localhost:3306/xyzDB}}
      username: ${XYZ_DATASOURCE_USERNAME:${XYZ_DB_USERNAME:cpf_xyz_app}}
      password: ${XYZ_DATASOURCE_PASSWORD:${XYZ_DB_PASSWORD:__REPLACE_BY_ENV__}}
