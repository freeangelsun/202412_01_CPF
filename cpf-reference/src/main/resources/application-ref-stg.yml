# REF/EDU stg profile 설정입니다.

server:
  port: ${REF_SERVER_PORT}

spring:
  datasource:
    ref:
      url: ${REF_DATASOURCE_URL}
      username: ${REF_DATASOURCE_USERNAME}
      password: ${REF_DATASOURCE_PASSWORD}
