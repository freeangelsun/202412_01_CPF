UPDATE bat_scheduler_lease
   SET lease_until = ?,
       last_heartbeat_at = @NOW6@,
       updated_at = @NOW6@
 WHERE scheduler_key = ?
   AND owner_instance_id = ?
   AND fencing_token = ?
   AND lease_until >= @NOW6@
