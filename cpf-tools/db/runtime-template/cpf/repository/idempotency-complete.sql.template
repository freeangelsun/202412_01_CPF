UPDATE CPF_IDEMPOTENCY_RECORD
SET record_status = ?,
    stored_response = ?,
    retry_allowed_yn = ?,
    completed_at = @NOW@,
    updated_by = 'CPF_IDEMPOTENCY',
    updated_at = @NOW@
WHERE scope = ?
  AND idempotency_key = ?
