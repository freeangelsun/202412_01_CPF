UPDATE OPS_OPERATION_POLICY
SET enabled_yn = 'N',
    change_reason = ?,
    updated_by = ?,
    policy_version = policy_version + 1,
    updated_at = @NOW@
WHERE operation_id = ?
  AND policy_version = ?
