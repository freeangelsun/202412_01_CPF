<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cpf.common.ref.mapper.CacheRefreshEventMapper">
    <insert id="insertEvent">
        INSERT INTO cpf_cache_refresh_event (
            cache_name,event_type,event_key,source_was_id,published_by,published_at,created_by,updated_by
        ) VALUES (
            #{cacheName},#{eventType},#{eventKey},#{sourceWasId},#{publishedBy},CURRENT_TIMESTAMP(3),#{publishedBy},#{publishedBy}
        )
    </insert>
    <select id="findMaxEventId" resultType="long">
        SELECT MAX(event_id) FROM cpf_cache_refresh_event
    </select>
    <select id="findEventsAfter" resultType="map">
        SELECT event_id AS eventId,cache_name AS cacheName,event_type AS eventType,event_key AS eventKey,
               source_was_id AS sourceWasId,published_by AS publishedBy,published_at AS publishedAt
        FROM cpf_cache_refresh_event
        WHERE event_id &gt; #{lastEventId}
        ORDER BY event_id
    </select>
    <select id="findCheckpoint" resultType="long">
        SELECT last_event_id FROM cpf_cache_refresh_checkpoint WHERE consumer_id=#{consumerId}
    </select>
    <insert id="insertCheckpoint">
        INSERT INTO cpf_cache_refresh_checkpoint (consumer_id,last_event_id,last_success_at,created_by,updated_by)
        VALUES (#{consumerId},#{lastEventId},CURRENT_TIMESTAMP(3),'CPF','CPF')
    </insert>
    <update id="updateCheckpoint">
        UPDATE cpf_cache_refresh_checkpoint
        SET last_event_id=#{lastEventId},last_success_at=CURRENT_TIMESTAMP(3),updated_at=CURRENT_TIMESTAMP(3),updated_by='CPF'
        WHERE consumer_id=#{consumerId} AND last_event_id &lt;= #{lastEventId}
    </update>
</mapper>
