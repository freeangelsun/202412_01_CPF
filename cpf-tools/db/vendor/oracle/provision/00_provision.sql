-- CPF generated lifecycle bundle; vendor=oracle
-- Source plan: cpf-tools/db/config/database-source-plan.json

-- ===== BEGIN 00_provision.sql =====
-- AUTO-GENERATED from cpf-tools/db/config/database-install.default.json
-- vendor=oracle; password tokens are rendered only in-memory by the executor.
-- DO NOT EDIT generated provision SQL directly.

-- CPF_LOGICAL_DATABASE=cpfDB
BEGIN
    EXECUTE IMMEDIATE 'CREATE USER CPFDB IDENTIFIED BY "${CPFDB_PASSWORD}"';
EXCEPTION
    WHEN OTHERS THEN
        IF SQLCODE != -1920 THEN RAISE; END IF;
END;
/
GRANT CREATE SESSION, CREATE TABLE, CREATE VIEW, CREATE SEQUENCE, CREATE TRIGGER, CREATE PROCEDURE TO CPFDB;
ALTER USER CPFDB QUOTA UNLIMITED ON USERS;

-- CPF_LOGICAL_DATABASE=cpfDB
BEGIN
    EXECUTE IMMEDIATE 'CREATE USER CPFDB IDENTIFIED BY "${CPFDB_PASSWORD}"';
EXCEPTION
    WHEN OTHERS THEN
        IF SQLCODE != -1920 THEN RAISE; END IF;
END;
/
GRANT CREATE SESSION, CREATE TABLE, CREATE VIEW, CREATE SEQUENCE, CREATE TRIGGER, CREATE PROCEDURE TO CPFDB;
ALTER USER CPFDB QUOTA UNLIMITED ON USERS;

-- CPF_LOGICAL_DATABASE=mbwDB
BEGIN
    EXECUTE IMMEDIATE 'CREATE USER MBWDB IDENTIFIED BY "${MBWDB_PASSWORD}"';
EXCEPTION
    WHEN OTHERS THEN
        IF SQLCODE != -1920 THEN RAISE; END IF;
END;
/
GRANT CREATE SESSION, CREATE TABLE, CREATE VIEW, CREATE SEQUENCE, CREATE TRIGGER, CREATE PROCEDURE TO MBWDB;
ALTER USER MBWDB QUOTA UNLIMITED ON USERS;

-- ===== END 00_provision.sql =====
