SELECT POLICY_HISTORY_ID, POLICY_CODE, POLICY_VERSION, CHANGE_TYPE, CHANGE_REASON, BEFORE_HASH, AFTER_HASH, OPERATOR_ID, CREATED_AT FROM adm_approval_policy_history WHERE POLICY_CODE = ? AND POLICY_VERSION = ? ORDER BY POLICY_HISTORY_ID;
