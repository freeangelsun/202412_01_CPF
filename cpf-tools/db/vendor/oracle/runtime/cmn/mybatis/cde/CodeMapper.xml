<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cpf.common.cde.mapper.CodeMapper">

    <!-- CPF 설정 설명입니다. -->
    <select id="findAllCodes" resultType="map">
        SELECT
            code_id,
            parent_id,
            code_key,
            code_value,
            description,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM cpf_code
        ORDER BY parent_id, code_key, code_value
    </select>

    <!-- CPF 설정 설명입니다. -->
    <select id="findCodeByKey" resultType="map" parameterType="string">
        SELECT
            code_id,
            parent_id,
            code_key,
            code_value,
            description,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM cpf_code
        WHERE code_key = #{codeKey}
        ORDER BY code_id
        FETCH FIRST 1 ROWS ONLY
    </select>

    <!-- CPF 설정 설명입니다. -->
    <select id="findCodesByKey" resultType="map" parameterType="string">
        SELECT
            code_id,
            parent_id,
            code_key,
            code_value,
            description,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM cpf_code
        WHERE code_key = #{codeKey}
        ORDER BY code_value, code_id
    </select>

    <!-- CPF 설정 설명입니다. -->
    <select id="findCodeById" resultType="map" parameterType="long">
        SELECT
            code_id,
            parent_id,
            code_key,
            code_value,
            description,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        FROM cpf_code
        WHERE code_id = #{codeId}
    </select>

    <!-- CPF 설정 설명입니다. -->
    <insert id="insertCode" parameterType="com.cpf.common.cde.dto.CommonCodeRequest"
            useGeneratedKeys="true"
            keyProperty="codeId"
            keyColumn="code_id">
        INSERT INTO cpf_code (
            parent_id,
            code_key,
            code_value,
            description,
            use_yn,
            created_by,
            created_at,
            updated_by,
            updated_at
        ) VALUES (
            #{parentId},
            #{codeKey},
            #{codeValue},
            #{description},
            COALESCE(NULLIF(#{useYn}, ''), 'Y'),
            COALESCE(NULLIF(#{requestUser}, ''), 'SYSTEM'),
            CURRENT_TIMESTAMP,
            COALESCE(NULLIF(#{requestUser}, ''), 'SYSTEM'),
            CURRENT_TIMESTAMP
        )
    </insert>

    <!-- CPF 설정 설명입니다. -->
    <update id="updateCode">
        UPDATE cpf_code
        SET
            parent_id = #{request.parentId},
            code_key = #{request.codeKey},
            code_value = #{request.codeValue},
            description = #{request.description},
            use_yn = COALESCE(NULLIF(#{request.useYn}, ''), 'Y'),
            updated_by = COALESCE(NULLIF(#{request.requestUser}, ''), 'SYSTEM'),
            updated_at = CURRENT_TIMESTAMP
        WHERE code_id = #{codeId}
    </update>

    <!-- CPF 설정 설명입니다. -->
    <delete id="deleteCode" parameterType="long">
        DELETE FROM cpf_code
        WHERE code_id = #{codeId}
    </delete>

</mapper>
