SELECT * FROM CPF_WEBHOOK_DELIVERY WHERE DELIVERY_STATE IN ('RETRY','DLQ','UNKNOWN') ORDER BY NEXT_ATTEMPT_AT, DELIVERY_SEQUENCE;
SELECT * FROM CPF_DATA_QUALITY_QUARANTINE WHERE QUARANTINE_STATE <> 'REPLAYED' ORDER BY UPDATED_AT;
