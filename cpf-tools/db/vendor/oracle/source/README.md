# ORACLE canonical generated source

이 디렉터리의 Schema SQL은 `cpf-tools/db/canonical/platform-schema.json`에서 생성됩니다.
직접 수정하지 않습니다. `generate-vendor-schema-from-canonical.ps1`로 재생성합니다.
`USE <logicalDB>;` 라인은 CPF packaging directive이며 DB client에 직접 전달되지 않습니다.
