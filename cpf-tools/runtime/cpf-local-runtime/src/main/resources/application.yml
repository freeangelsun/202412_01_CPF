spring:
  profiles:
    default: local,local-integrated
  application:
    name: cpf-local-runtime
  main:
    banner-mode: off
server:
  address: 127.0.0.1
  port: ${CPF_LOCAL_RUNTIME_PORT:8080}
cpf:
  logging:
    # 일반 Runtime 로그는 Application/Instance별 경로로 분리하며 Transaction Evidence와 혼용하지 않습니다.
    enabled: true
    root: ${CPF_LOG_ROOT:logs}
    instance-id: ${CPF_RUNTIME_INSTANCE_ID:${HOSTNAME:local}}
    maintenance-interval: ${CPF_LOG_MAINTENANCE_INTERVAL:1h}
    files:
      runtime:
        # 전체 Runtime 흐름을 기록하는 기본 파일입니다. Console 출력도 함께 유지됩니다.
        enabled: true
        file-name: runtime.log
        rolling: DAILY
        compress-after-days: 5
        delete-after-days: 365
      error:
        # ERROR 이상을 별도 파일에도 기록하여 운영 장애 탐색 경로를 명확히 합니다.
        enabled: true
        file-name: error.log
        level: ERROR
        rolling: DAILY
        compress-after-days: 5
        delete-after-days: 365
  environment: local
  # Canonical systemCode는 명시 선언이 정본이며 profile/application 이름으로 대체하지 않습니다
  # (CpfRuntimeSystemCode 계약). 통합 로컬 Runtime은 여러 App을 한 JVM에 올리는 개발/검증용
  # 실행 단위이므로 자신의 코드를 LOCAL로 선언하고, 환경변수로 덮어쓸 수 있게 둡니다.
  system-code: ${CPF_SYSTEM_CODE:LOCAL}
  local:
    runtime:
      enabled: false
      allow-remote-bind: false
      max-enabled-modules: 6
      minimum-max-memory-mb: 250
