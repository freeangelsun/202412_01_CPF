[pytest]
addopts = --import-mode=importlib -p no:cacheprovider
# CPF는 동일 basename의 owner별 test module을 허용하므로 importlib collection을 canonical로 사용합니다.
# Product source에 .pytest_cache/__pycache__를 만들지 않는 fail-clean verification을 기본값으로 둡니다.
