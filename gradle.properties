# Project cache ownership is absolute and entrypoint-specific: settings.gradle owns Tooling API/IDE,
# while both Gradle wrappers pass --project-cache-dir for command-line execution.
# A relative org.gradle.projectcachedir here would be inherited and recreated below every subproject.
