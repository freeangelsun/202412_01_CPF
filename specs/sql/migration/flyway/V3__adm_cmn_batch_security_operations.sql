-- V3: ADM/CMN/배치 운영 보강 migration입니다.
-- 현재 공식 설치본에서는 V1 baseline에 동일 구조가 포함되어 있습니다.
-- 기존 DB 업그레이드는 specs/sql/00_all_install.sql 또는 후속 V4 migration으로 별도 검증합니다.

SELECT 'V3 baseline absorbed' AS migration_note;
